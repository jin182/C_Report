### 3. 두 실수를 배열에 초기화해서 저장하고 더한 후 화면에 출력 하는 C프로그램을 작성하시오.

---

소스코드
```
#include <stdio.h>

int main(void) {
	double arr[] = { 1.1, 2.2 };
	double sum = 0;
	sum = arr[0] + arr[1];
	printf("%lf\n", sum);
}
```
실행결과
```
3.300000
```
---
주의사항 : index는 0부터 시작됨
